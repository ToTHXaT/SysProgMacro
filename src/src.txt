
m1 1, 2
mov ax, bx

m1 macro a, b, c=1
    mov  $a, $b, $c
    word 'Hello, world'
    m3 macro
        mov ax, bx
        jmp L1
    mend
mend

mov bx, cx

m2 macro a, b, c=1
    mov cx, dx
    byte 1
    word 'Hello, world'
mend
