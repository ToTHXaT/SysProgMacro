m1 macro a, b, d=
    mov $a, $b
    m2 macro c
        word $d
        byte $c
    mend
    if $a > 10
        jmp l1
    else
        jmp l2
    endif
    mov $b, $a
    m2 "Hello, world from m2"
mend

m1 ax, bx, d=5

m3 macro a, b
mend

m3 1,2