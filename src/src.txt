m1 11, 20, d=5

m1 macro a, b, d=
    mov $a, $b
    m2 macro c
        word $d
        byte $c
    mend
    if $a > 10
        jmp l1
        if $b == 20
            byte 'b == 20'
        endif
        jmp l1
    else
        jmp l2
    endif
    mov $b, $a
    m2 "Hello, world from m2"
mend


mm1 macro a
    while $a < 10

        l1 byte $a

        inc a
    wend
mend

mm1 0